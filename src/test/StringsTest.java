package src.test;

import src.main.java.com.example.ManipulatingStrings;
import org.junit.Test;
import static org.junit.Assert.assertEquals;

public class StringsTest {
@Test
public void testManipulatingStrings() {
      // Example test case
      String input = "Hello, World!";
      String expectedOutput = "Hello, World!"; // Replace with the expected output of your method
      
      // Call the method you want to test (replace with actual method call)
      String actualOutput = input; // Replace with actual method call
      
      // Assert the expected output
      assertEquals(expectedOutput, actualOutput);
}

}
